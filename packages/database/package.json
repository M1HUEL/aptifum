{
  "name": "@aptifum/database",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "lint:fix": "eslint src --fix --max-warnings 0",
    "migration:generate": "typeorm-ts-node-esm migration:generate -d src/data-source.ts src/migrations/Migration",
    "migration:revert": "typeorm-ts-node-esm migration:revert -d src/data-source.ts",
    "migration:run": "typeorm-ts-node-esm migration:run -d src/data-source.ts",
    "seed": "tsx src/seeders/seed.ts",
    "seed:demo": "tsx src/seeders/seed-demo.ts",
    "seed:demo:transactions": "tsx src/seeders/seed-demo-transactions.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aptifum/config": "workspace:*",
    "@aptifum/core": "workspace:*",
    "bcryptjs": "^3.0.3",
    "pg": "^8.22.0",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}
